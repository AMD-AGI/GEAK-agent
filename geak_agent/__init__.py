"""GEAK Agent - GPU Evolutionary Agent for Kernel optimization.

A true agent (like minswe) for kernel optimization with:
- Discovery pipeline (runs first)
- MCP tools (kernel-ercs, kernel-profiler, kernel-evolve)
- Agent loop (LLM decides actions via bash + MCP tools)
"""

__version__ = "0.1.0"
